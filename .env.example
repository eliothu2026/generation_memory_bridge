# ==============================
# 复制此文件为 .env，填入真实值：
#   cp .env.example .env
# .env 已在 .gitignore 中，不会被提交。
#
# 说明：本项目只用到下面这几个环境变量（都与 LLM / 考据 agent 有关）。
# 纯文本 demo 与免 key 回放（python -m narrator_flow.main --demo）不需要任何 key。
# ==============================

# ---- DeepSeek（除 --demo 回放外必填）----
# 三条流水线与考据 agent 都直连 DeepSeek 的 OpenAI 兼容接口（无 agent 框架）。
DEEPSEEK_API_KEY=sk-...
# 接口地址，可选；不设则默认为 https://api.deepseek.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ---- 维基百科检索端点（可选，供考据 agent 核实史实）----
# 仅在真实运行（有 key）时按节奏触发；不设则默认 https://zh.wikipedia.org/w/api.php。
# 维基在中国大陆被屏蔽，访问不到时可指向镜像/代理；缺失或不可达会安全回退，不影响主流程。
# WIKIPEDIA_API_ENDPOINT=https://zh.wikipedia.org/w/api.php
