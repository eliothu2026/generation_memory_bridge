# ==============================
# 使用说明：
# 复制此文件为 .env，填入真实值
# 命令：cp .env.example .env
# 注意：.env 已加入 .gitignore，不会被提交
# ==============================

# ---- 应用基础配置 ----
APP_ENV=development          # development | production | test
APP_PORT=8000
APP_SECRET_KEY=your_secret_key_here

# ---- 数据库 ----
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# 或 MySQL：mysql://user:password@localhost:3306/dbname
# 或 SQLite：sqlite:///./app.db

# ---- Redis（可选）----
REDIS_URL=redis://localhost:6379/0

# ---- 前端（Node.js）----
NEXT_PUBLIC_API_URL=http://localhost:8000
NODE_ENV=development

# ---- narrator_flow (CrewAI Flows demo) ----
# 当前 agents.yaml 中配置使用 DeepSeek（llm: deepseek/deepseek-chat），
# CrewAI 底层基于 litellm，会读取 DEEPSEEK_API_KEY 环境变量
DEEPSEEK_API_KEY=sk-...

# 若改回使用 OpenAI 模型，取消注释并将 agents.yaml 中 llm 改为 gpt-4o-mini
# OPENAI_API_KEY=sk-...

# ---- 第三方 API 密钥 ----
# STRIPE_SECRET_KEY=sk_test_...
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# ---- 邮件服务 ----
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
